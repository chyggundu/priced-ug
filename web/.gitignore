node_modules/

# Next.js
.next/
out/
next-env.d.ts

# Env
# Tracked on purpose: these hold only NEXT_PUBLIC_* values, which are compiled
# into the static site and downloaded by every visitor, so tracking them adds no
# exposure. The negations override the repo-root .gitignore.
.env
!.env.local
!.env.production.local

# Misc
.DS_Store
*.tsbuildinfo
.vercel
