# Intentionally empty.
#
# This file makes `web/` its own pnpm workspace root. Without it, pnpm would walk
# up and adopt the monorepo root at ../pnpm-workspace.yaml, pulling in its catalog
# and settings. The website must stay fully independent of the mobile app packages.
packages: []
