{
  "name": "priced-ug",
  "private": true,
  "version": "0.0.0",
  "license": "MIT",
  "//": "Not a workspace. `mobile` (artifacts/pricedug) and `web` are fully independent apps — each owns its package.json, lockfile, node_modules and tsconfig, and each talks to Supabase directly. Install and run them from inside their own directory."
}
